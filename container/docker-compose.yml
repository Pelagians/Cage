# WinForge Docker Compose — local development and testing
#
# Build a specific provider image, then run the builder or a shell inside it.
# Examples:
#   docker compose build wine
#   docker compose run wine bash
#   docker compose run --rm wine winforge build /workspace/manifest.json

services:
  wine:
    build:
      context: ..
      dockerfile: container/providers/wine/Dockerfile
      args:
        WINE_PACKAGE_VERSION: "11.0.0.0~bookworm-1"
    image: winforge/wine:11.0-dev
    environment:
      WINEFS: builder
    volumes:
      - ..:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true

  wine-staging:
    build:
      context: ..
      dockerfile: container/providers/wine-staging/Dockerfile
      args:
        WINE_PACKAGE_VERSION: "11.10~bookworm-1"
    image: winforge/wine-staging:11.10-dev
    environment:
      WINEFS: builder
    volumes:
      - ..:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true

  umu-proton-ge:
    build:
      context: ..
      dockerfile: container/providers/umu-proton-ge/Dockerfile
      args:
        GE_PROTON_TAG: "GE-Proton11-1"
    image: winforge/umu-proton-ge:GE-Proton11-1-dev
    environment:
      WINEFS: builder
    volumes:
      - ..:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
