schemaVersion: cage.app/v0
name: notepadplusplus
version: "0.1.0"

runtime:
  provider: wine
  version: "11.0"
  network: none

build:
  network: bridge

modules:
  - type: chocolatey
    install:
      packages:
        - notepadplusplus

launch:
  entrypoint: C:/Program Files/Notepad++/notepad++.exe
